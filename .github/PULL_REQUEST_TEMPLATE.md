## What

## Why

## Testing

- [ ] `bash tests/run.sh` passes
- [ ] `shellcheck -x -P scripts -P tests scripts/cprof scripts/lib/*.sh hooks/*.sh statusline/*.sh tests/*.sh install.sh` is clean
- [ ] New behavior carries an assertion in `tests/`
